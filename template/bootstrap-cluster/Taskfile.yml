version: '3'
vars:
  ENV: {{ "'{{.ENV}}'" }}

includes:
  talos:
    taskfile: ./talos.yaml
    vars:
      ENV: {{ "'{{.ENV}}'" }}

tasks:
  default:
    desc: |
      List all available tasks in the Taskfile.
    cmds:
      - task --list
